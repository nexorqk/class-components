describe('API', () => {
  describe('Mocked API Calls', () => {
    it('Use jest.mock, or vi.mock, or MSW to mock API calls', () => {
      // https://mswjs.io/docs
    });

    it('Test both success and error scenarios', () => {});
  });
});
